[
  {
    "id": "df-build",
    "title": "crate builds (cargo build)",
    "kind": "command",
    "description": "testsprite-rs must compile.",
    "code": "cargo build --quiet"
  },
  {
    "id": "df-test",
    "title": "unit tests pass (cargo test)",
    "kind": "command",
    "description": "The in-crate #[cfg(test)] suite must be green.",
    "code": "cargo test --quiet"
  },
  {
    "id": "df-clippy",
    "title": "clippy clean, zero warnings",
    "kind": "command",
    "description": "Project standard: cargo clippy --all-targets at zero warnings.",
    "code": "cargo clippy --all-targets -- -D warnings"
  },
  {
    "id": "df-generation-regressions",
    "title": "generation and auth-flow regression tests stay green",
    "kind": "command",
    "description": "Pins TestSprite PRD/test generation seams: Responses-only models route correctly, LLM planner prompts keep OAuth/env-driven runnable cases, malformed generated command cases are lint-blocked, code summaries generate deterministic specs without LLM, and command tests inherit .testsprite.env variables.",
    "code": "cargo test responses_api --quiet && cargo test prompt --quiet && cargo test command_tests_require_code_not_steps --quiet && cargo test from_code_summary_with_endpoints_generates_deterministic_specs_without_llm --quiet && cargo test injects_testsprite_variables_into_command_env --quiet"
  }
]
